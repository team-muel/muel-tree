# public/illust — 인게임 일러스트 에셋

원화는 여기 self-host 한다 (Discord Activity CSP — 외부 CDN 은 url-mapping 없이 차단됨).

드롭 절차: `src/config/illustrations.ts` 상단 doc 참조 (파일 추가 1줄 + 매니페스트 등록 1항목).
권장: 가로 ≤1920px, ≤1.5MB. next/image 가 서빙 시 webp/avif 로 최적화한다.
